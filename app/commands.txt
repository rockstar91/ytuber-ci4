wget \
     --recursive \
     --no-clobber \
     --page-requisites \
     --html-extension \
     --convert-links \
     --restrict-file-names=windows \
     --domains themes.startbootstrap.com \
     --no-parent \
         https://themes.startbootstrap.com/sb-admin-pro/

